= form_for @m_question do |f|
  - if @m_question.errors.any?
    #error_explanation
      %h2= "#{pluralize(@m_question.errors.count, "error")} prohibited this m_question from being saved:"
      %ul
        - @m_question.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :content
    = f.text_area :content
  .actions
    = f.submit 'Save'
